{
	"$schema": "../../schemas/Aurora.card.schema.json",
	"id": "CNS-016",
	"card_type": "Constraint",
	"name": "Symlink and Junction-Safe Boundary Checks",
	"description": "Workspace boundary enforcement MUST be robust to symlinks/junctions and path traversal tricks; boundary checks MUST use canonicalized/resolved paths and treat path inputs as hostile.",
	"attributes": {
		"treat_as_user_intent": true,
		"must_enforce_boundary_after_resolution": true,
		"platform_notes": {
			"windows": {
				"junctions_must_be_validated": true
			}
		},
		"error_on_escape": {
			"kind": "permission_denied",
			"message": "symlink/junction escapes workspace"
		}
	},
	"links": [
		{
			"relationship": "limits",
			"target": "REQ-003"
		}
	]
}
