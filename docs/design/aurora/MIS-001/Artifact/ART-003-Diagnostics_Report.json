{
	"$schema": "../../schemas/Aurora.card.schema.json",
	"id": "ART-003",
	"card_type": "Artifact",
	"card_subtype": "Report",
	"name": "Diagnostics Report",
	"description": "Normalized diagnostics output emitted by build/test/lint/format tooling, optionally with deltas between runs.",
	"links": [],
	"attributes": {
		"format": ["json", "json"],
		"schema": "schemas/alfred.diagnostics.schema.json",
		"sources": ["cargo", "pnpm", "linters", "formatters"],
		"contracts": {
			"protocol": "docs/design/Protocol.md",
			"errors": "docs/design/ErrorTaxonomy.md"
		},
		"notes": [
			"When emitted as json, each line is a complete JSON object and the stream is append-only.",
			"Diagnostics envelopes are designed to be validated against a deterministic schema and taxonomy."
		]
	}
}
